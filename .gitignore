node_modules/
.next/
out/
build/

.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Исходники для npm run cutout — в репозиторий не нужны, в сборку не попадают.
raw-photos/

# Временные тестовые снимки: это каталожные фотографии чужих марок, они не
# должны попасть в историю репозитория. Настоящие фото называйте без префикса
# _demo- — они будут отслеживаться как обычно.
public/products/_demo-*
